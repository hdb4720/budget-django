# Budget Manager Logic

## Objectives
Presentation of budget rows

## Selected-period behavior
-- if selected-period includes today
    -- Select all budgets where due_date >= period.start
-- else
    -- Select all budgets where due_date between period.start and period.end

## Future-budget behavior
-- Planned budgets only in future projections:
    -- pros:
        -- does not clutter the manager 
    -- cons:
        -- could be valuable for budget planning

-- do not include in manager.actual calculations

## Budget summarization
-- Budget records kept as is in budgets
-- Manager groups budgets by:
    -- period:
        -- periods has all periods for the selected scheme
        -- include a period.id in manager row
    -- type:
        -- keep Planned and Transient separate
            -- example: 
                -- budget groceries as Transient once a week at $100
                -- budget Thanksgiving groceries as planned for 11/20
    -- category
    -- account
    -- due_date:
        -- if exists min(due_date)>=today else: max(due_date)
            -- example:
                -- Groceries
                    -- 4 budgets - mm/7 mm/14 mm/21 mm/28
                    -- today - mm/15
                        -- previous is past, move on to next budget
                        -- due_date = min(due_date)>=mm/15: mm/21
                    -- today - mm/29: due_date = mm/21 - desired: mm/21
                        -- previous is past, no next budget
                        -- due_date = min(due_date)>=mm/29: mm/28
                -- Double bill 
                    --scenario: Gas bill due mm/26; current period mm-1/22-mm/28 
                    -- 2 budgets mm-1/26 mm/26
                    -- today mm-1/22
                        -- due_date = min(due_date)>=m-1/22 = mm-1/26
                    -- today mm/1
                        -- due date = min(due_date>=mm/1 = mm/26)
                -- Regular budget
                    -- scenario: mortgage due_date mm/5; current period mm-1/22-mm/28 
                    -- today mm/1
                        -- due_date = min(due_date)>=mm/1 = mm/5
                    -- today mm/6
                        -- due_date = min(due_date)>=mm/5 = None: max(due_date) = mm/5
    -- sum(amount)
    -- list(budget.id)
-- Manager display summarized period budget 
    -- Details:
        -- Requirements:
            -- Disable Selector while Details is open
        -- if multiple budgets (len(budget.id)>1)
            -- modal screen to select among budgets
        -- else
            -- select budget
        -- modal screen with CRUD
            -- if len([budget.id]) >= 1
                -- Edit: edit selected row 
                    -- editable fields: category, acount, due_date, amount
                -- Delete: delete selected row
                -- Add: use current row as a template to create a new row
                    -- inherited fields: category, account, type (editable)
                    -- due_date: None
                        -- Possibly include recurrence generator
                            -- recurrence options
                                -- days
                                -- weeks
                                -- months
                                -- years
                        -- Calendar widget
                    -- amount: None
            -- else: (phantom row)
                -- Add
            -- Other options:
                -- Transaction/Budget display
                    -- Show a modal screen of transactions/budgets for this category
                        -- default date range: period range of selected row
                            -- allow for date range selection with wide open date range
                        -- order by due_date, trn_date
                            :Precedence due_date on groups of mixed budget/transactions on same date
                
## Status rules
Planned status:
pending: due date set, in period, today or in the future; actual=0
partial: due date set, in period, today or in the future; actual<0
overpaid: due date set, in period; actual < amount
overdue: due date set, in period, in past; actual > amount
future: due date set, after period
 
Transient status:
under: actual > amount
met: actual = amount
over: actual < amount

 inactive: a budget with no history
    

## Unresolved questions

## Example scenarios

## Samples

## Discussion
- 9/4/26:07:12
    - The grain problem
        - grouping by Period
            - will always be the same Scheme as the selected scheme
            - adding period.id to a budget row is a temporary grouping valid for this view only
                - this will allow future budgets to segregate away from current budgets for historical analysis.
                    - historical analysis will still be based on selected period.
                        - transactions <= period.end
                            - min, max, avg covering from begiining of time to period.end
                            - actual: transactions within the period.start:period.end
                    - future budgets:beyond selected period, will have no historical data attached except: last_date, last_amount. They basically functions as reminders. status could be set as a "reminder".
        - grouping by Category
            - this is the primary grouping for all budgets within the selected period. any budgets in the selected period are grouped by Category
        - grouping should be Category-Period
        - grouping by Type and Account
            - you are exactly right that Type and Account should not cause any segregtion. I think though that the manager type and account should be highlighted in some way so that the reader can identify if a budget row has "mixed" type and/or account. We could even substitute in MIXED for either Type or Account when they are mixed.
                - Type can be mixed, though not often, for various reasons:
                    - the Groceries with special Thanksgiving budget
                        - I can plan on spending $100 a week on Groceries and in later analysis I will be interested in tracking how well I met the budget for Groceries
                        - By the same token since I know I will have a special Groceries list for Thanksgiving I can add the secondary line and then it will be my respnsibility in reviewing of budgets and transactions to see the impact of the special budget.
                    - this could also apply to a desire to get ahead of a credit card debt. I know I have to pay the minimum due on time, but if I wanted to try to plan for an extra payment. I could set up the Planned due on due_date and set a Transient for the extra $100. Again works as a reminder that if psossible make a special payment.
                - Account in the budget is a "suggestion" of which account to pay from. it could be from Checking, Savings or Credit, I may not have a determination at the beginning of a period and determine later in the period which Account to use. 
                    - I have not defined othe management views yet but certainly one of the dashboard elements, maybe even as a modal on the budget-manager screen to show current Account balances from Account/Transactions and assigned budget totals to keep track of each account having sufficient funds available to meet the budget. So yes a particular category may have budgets slated to pay out of more then one account and even not assigned to an account yet.
                        - Along these lines I would envision a modal that has columns for Account, Date, Actual, Budget, Net for the current period. A quick look to see that I am properly funded or if I need to adjust some account balances to meet my budget or adjust account on budgets to meet my budget.


- 9/4/26:09:27
    - I think I could successfully argue that any Planned budget among a group of budgets would refer that category to transaction-history not period-history.
        - I see your argument for the reverse: period-history for mixed types.
            - I can see how that is a valid insight about how to interpret min, max and avg values. Using individual could easily mask out that in prior periods there was also mixed budgetting involved where the minimum due was coupled with add-on payments. Using the period-histories the min, max, avg would properly reflect the application of the add-on.
        - I stand corrected and agree with your recommendation: mixed type will use period-histories
    
