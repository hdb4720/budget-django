To Do List
# Done Truncate category name to CHAR(30)
1) Normalize Min/Max for expenses

Next steps

1) Add column sorting 
    1) Clicking the Category heading sorts Category ascending without losing the selected scheme, period, or selected date.
    2) 
2) Add column filtering
3) Add stickey selector/header with on/off control
4) Add row selection and highlighting.
5) Add row-detail inspector.
6) Add CRUD
