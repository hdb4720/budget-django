## Synopsis
Define the presentation row of budget-manager
## Objectives
Group the manager rows into single category-period rows linking back to manager rows.

## Discussion
Now I have a new challenge to put before us. build_manager seems to be complete and accurate, though future auditing needs to stay in the path to provide a higher level of confidence in it's acuracy. 

Except what I have built is not what needs to feed the presentation of the budget-manager. At this point manager is still segregated into individual budget with the phantom budgets - those categories with history, but without a budget. I need to define the final phase of presentation data that will appear to the user. Behind each presentation row will be the manager rows, but presentation will group category-period into single integrated rows with the adjusted due_date rules applied. Also it is obvious that the history needs to migrate to the presentation level as well since the historical analysis shows history for the category-period not the individual budget rows.

My first issue to resolve is the structure of a presentation row. I believe we need to define a class - BudgetPresentationRow - that will incorporate a unique id for linking purposes. The structure of the presentation will be almost identical to what I have done in manager, in fact several pieces of manager need to be cut from manager and pasted into presentation. My initial problem is manager is a list providing little internal capapbility to query and group data.

I need help to develop a resolution for this issue.

    - do you agree that Presentation is the logical step for Manager in the same way that Manager was the next logical step for Budget and Phantom

    - will a class structure give me a unique id that I can use for linking or should I rely on the (category, period) to act as primary key?

    - how can I utilize the existing [manager] to:
        - group (category, period) manager rows
        - provide a link from presentaiton to manager to use when the user has to choose between rows for CRUD and persistence activities?





## Unresolved questions

## Example scenarios

## Samples

```python
if period.end < today:
    ...
```